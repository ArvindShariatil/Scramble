{
  "name": "scramble-game",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A calm, anxiety-free anagram word puzzle game designed for relaxation and mental wellness",
  "author": "Scramble Game Team",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "npm run clean && npm run lint && tsc && vite build",
    "build:dev": "tsc && vite build --mode development",
    "build:analyze": "npm run build && npx vite-bundle-analyzer dist",
    "build:size": "npm run build && powershell -Command \"Get-ChildItem -Path dist -Recurse | Measure-Object -Property Length -Sum | Select-Object @{Name='SizeKB';Expression={[math]::Round($_.Sum/1KB,2)}}\"",
    "preview": "vite preview",
    "preview:host": "vite preview --host",
    "test": "vitest",
    "test:prod": "vitest run",
    "test:coverage": "vitest --coverage",
    "test:ui": "vitest --ui",
    "lint": "tsc --noEmit && echo 'TypeScript linting complete ✅'",
    "type-check": "tsc --noEmit",
    "clean": "rmdir /S /Q dist 2>nul || echo \"Clean complete\"",
    "validate": "npm run lint && npm run test:prod && npm run build:size",
    "deploy:vercel": "npm run validate && vercel --prod",
    "deploy:netlify": "npm run validate && netlify deploy --prod --dir=dist",
    "deploy:github": "npm run validate && gh-pages -d dist",
    "postbuild": "echo 'Build completed successfully! Ready for deployment 🚀'"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@typescript-eslint/eslint-plugin": "^8.48.0",
    "@typescript-eslint/parser": "^8.48.0",
    "eslint": "^9.39.1",
    "happy-dom": "^20.0.10",
    "typescript": "~5.9.3",
    "vite": "npm:rolldown-vite@7.2.5",
    "vitest": "^4.0.13"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@7.2.5"
  }
}
