# Sramble
Web based gaming app

## 📁 Project Structure

```
Sramble/
├── index.html              # Main entry point
├── package.json            # Project dependencies and scripts
├── .gitignore             # Git ignore rules
│
├── src/                   # Source code
│   ├── css/              # Stylesheets
│   │   └── style.css     # Main stylesheet
│   ├── js/               # JavaScript files
│   │   └── index.js      # Main JavaScript file
│   ├── components/       # Reusable game components
│   └── utils/            # Utility functions
│
├── public/               # Static assets
│   ├── images/          # Game images and graphics
│   ├── audio/           # Sound effects and music
│   └── fonts/           # Custom fonts
│
├── tests/               # Test files
└── docs/                # Documentation
```

## 🚀 Getting Started

### Prerequisites
- A modern web browser
- Node.js (optional, for development server)

### Installation

1. Clone the repository:
```bash
git clone https://github.com/ArvindShariatil/Sramble.git
cd Sramble
```

2. (Optional) Install dependencies:
```bash
npm install
```

### Running the Application

#### Option 1: Direct File Access
Simply open `index.html` in your web browser.

#### Option 2: Development Server
```bash
npm start
```
Then navigate to `http://localhost:8080` in your browser.

## 📝 Development

### Adding Your Code
- Place your HTML in `index.html`
- Add CSS styles in `src/css/`
- Add JavaScript files in `src/js/`
- Place images in `public/images/`
- Place audio files in `public/audio/`

### Project Guidelines
- Keep components modular and reusable in `src/components/`
- Use utility functions in `src/utils/` for common operations
- Write tests in `tests/` directory
- Document your code and features in `docs/`

## 🤝 Contributing
Contributions are welcome! Please feel free to submit a Pull Request.

## 📄 License
ISC
